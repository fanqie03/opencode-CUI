<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.opencode.cui.gateway.repository.AkSkCredentialRepository">

    <resultMap id="AkSkCredentialResultMap" type="AkSkCredential">
        <id     property="id"          column="id"/>
        <result property="ak"          column="ak"/>
        <result property="sk"          column="sk"/>
        <result property="userId"      column="user_id"/>
        <result property="description" column="description"/>
        <result property="status"      column="status"
                typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
        <result property="createdAt"   column="created_at"/>
        <result property="updatedAt"   column="updated_at"/>
    </resultMap>

    <select id="findActiveByAk" resultMap="AkSkCredentialResultMap">
        SELECT * FROM ak_sk_credential
        WHERE ak = #{ak}
          AND status = 'ACTIVE'
        LIMIT 1
    </select>

    <select id="findById" resultMap="AkSkCredentialResultMap">
        SELECT * FROM ak_sk_credential WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ak_sk_credential
            (ak, sk, user_id, description, status, created_at, updated_at)
        VALUES
            (#{ak}, #{sk}, #{userId}, #{description}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="updateStatus">
        UPDATE ak_sk_credential
        SET status = #{status}, updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>
