{
  "name": "@opencode-cui/pc-agent",
  "version": "1.0.0",
  "description": "PCAgent plugin — bridges local OpenCode with the remote AI-Gateway via WebSocket",
  "main": "plugin.ts",
  "types": "plugin.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@opencode-ai/sdk": "^1.2",
    "ws": "^8"
  },
  "devDependencies": {
    "@types/ws": "^8",
    "@types/node": "^20",
    "typescript": "^5"
  }
}
