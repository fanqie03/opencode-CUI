# Plan 08.1-01 Summary

## Outcome

完成 `skill-server` 的 Layer1 `welinkSessionId` 输出收口：

- `SkillStreamHandler` 不再把无法解析的 `sessionId` 回写成字符串型 `welinkSessionId`
- `GatewayRelayService.sendInvokeToGateway()` 不再在 `create_session` 报文里输出字符串型 `welinkSessionId`
- `GatewayRelayServiceTest` 的 gateway 上行样例统一改为数字型 `welinkSessionId`

## Key Files

- `skill-server/src/main/java/com/opencode/cui/skill/ws/SkillStreamHandler.java`
- `skill-server/src/main/java/com/opencode/cui/skill/service/GatewayRelayService.java`
- `skill-server/src/test/java/com/opencode/cui/skill/service/GatewayRelayServiceTest.java`

## Verification

- `cd skill-server && mvn test "-Dtest=SkillStreamHandlerTest,SkillSessionControllerTest,SkillMessageControllerTest,GatewayRelayServiceTest"` 通过

## Notes

- 输入解析层仍允许从 JSON 数字节点读取 `welinkSessionId`
- 输出层统一只发数字或 `null`，不再回退为字符串
