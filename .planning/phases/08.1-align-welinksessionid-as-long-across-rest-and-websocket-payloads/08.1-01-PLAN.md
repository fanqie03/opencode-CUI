---
phase: 08.1
plan: 1
title: skill-server layer1 payload normalization
wave: 1
depends_on: []
requirements:
  - SESS-01
  - SAFE-02
files_modified:
  - skill-server/src/main/java/com/opencode/cui/skill/model/
  - skill-server/src/main/java/com/opencode/cui/skill/service/
  - skill-server/src/main/java/com/opencode/cui/skill/ws/
  - skill-server/src/test/java/com/opencode/cui/skill/
autonomous: true
user_setup: []
---

<objective>
先收紧 `skill-server` 的 Layer1 对外协议，保证 REST、历史消息 DTO、snapshot 与实时 WebSocket 消息中的 `welinkSessionId` 都只输出数字型，为后续跨服务链路收口提供稳定基线。
</objective>

<context>
@.planning/phases/08.1-align-welinksessionid-as-long-across-rest-and-websocket-payloads/08.1-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>
  <task type="auto" tdd="true">
    <name>Task 1: 清理 Layer1 DTO 与消息映射中的字符串回退</name>
    <files>
      skill-server/src/main/java/com/opencode/cui/skill/model/
      skill-server/src/main/java/com/opencode/cui/skill/service/ProtocolMessageMapper.java
      skill-server/src/main/java/com/opencode/cui/skill/ws/SkillStreamHandler.java
    </files>
    <behavior>
      - Test 1: `ProtocolMessageView`、历史消息与 snapshot 消息中的 `welinkSessionId` 序列化后是数字节点
      - Test 2: `SkillStreamHandler` 构建的 WebSocket 消息不再把可解析的 session id 写回字符串
      - Test 3: 对外 JSON 中不再出现 `\"welinkSessionId\":\"42\"` 这类输出
    </behavior>
    <action>将 `skill-server` 中对外协议 DTO 和消息映射逻辑统一到 `Long welinkSessionId`，去掉“parse 失败时退回字符串输出”的分支；必要时把内部读取逻辑改为先解析、失败则置空或走显式错误路径，而不是污染对外协议。</action>
    <verify>
      <automated>mvn -pl skill-server test -Dtest=SkillStreamHandlerTest,SkillSessionControllerTest,SkillMessageControllerTest,GatewayRelayServiceTest</automated>
    </verify>
    <done>`skill-server` 对 Miniapp 暴露的 REST 与 WebSocket 消息都以数字型 `welinkSessionId` 输出。</done>
  </task>

  <task type="auto" tdd="true">
    <name>Task 2: 收紧 gateway 回流到 skill-server 的解析与断言</name>
    <files>
      skill-server/src/main/java/com/opencode/cui/skill/service/GatewayRelayService.java
      skill-server/src/main/java/com/opencode/cui/skill/service/OpenCodeEventTranslator.java
      skill-server/src/test/java/com/opencode/cui/skill/service/
    </files>
    <behavior>
      - Test 1: 处理 gateway 上行 `tool_event`、`tool_done`、`tool_error`、`session_created` 时可稳定解析数字型 `welinkSessionId`
      - Test 2: 创建发送给 gateway 的 invoke JSON 时，`welinkSessionId` 为数字节点
      - Test 3: 相关测试用例改为断言数字 JSON，而不是字符串 JSON
    </behavior>
    <action>梳理 `GatewayRelayService` 与事件翻译器对 `welinkSessionId` 的读取和写入，明确哪些地方仅做输入容错，哪些地方属于协议输出；更新现有测试夹具和断言，使其反映数字型基线。</action>
    <verify>
      <automated>mvn -pl skill-server test -Dtest=GatewayRelayServiceTest</automated>
    </verify>
    <done>`skill-server` 与 `gateway` 的协议边界上，不再有“输出字符串型 `welinkSessionId`”的实现或测试预期。</done>
  </task>
</tasks>

<must_haves>
- `skill-server` 对外输出的 `welinkSessionId` 必须统一为数字型
- 任何字符串兼容仅允许停留在输入解析层，不能反向泄漏到输出 JSON
- 关键测试必须显式断言 JSON 数字节点，而不是 Java 对象值
</must_haves>

<success_criteria>
- Layer1 REST 响应中的 `welinkSessionId` 全部为数字
- Layer1 WebSocket 消息中的 `welinkSessionId` 全部为数字
- `skill-server` 相关测试不再依赖字符串型 `welinkSessionId`
</success_criteria>
