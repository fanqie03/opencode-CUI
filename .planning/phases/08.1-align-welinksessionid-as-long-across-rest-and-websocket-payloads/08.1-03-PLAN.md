---
phase: 08.1
plan: 3
title: miniapp contract tightening and regression documentation
wave: 3
depends_on:
  - 1
  - 2
requirements:
  - SAFE-02
files_modified:
  - skill-miniapp/src/
  - documents/
  - .planning/REQUIREMENTS.md
autonomous: true
user_setup: []
---

<objective>
在后端和中转链路收口后，清理 `skill-miniapp` 与项目文档中的宽松类型定义和旧示例，形成一套可回归验证的最终基线。
</objective>

<context>
@.planning/phases/08.1-align-welinksessionid-as-long-across-rest-and-websocket-payloads/08.1-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>
  <task type="auto" tdd="true">
    <name>Task 1: 清理 miniapp 中的宽松类型与适配逻辑</name>
    <files>
      skill-miniapp/src/protocol/types.ts
      skill-miniapp/src/utils/api.ts
      skill-miniapp/src/hooks/useSkillStream.ts
    </files>
    <behavior>
      - Test 1: 前端协议类型中不再存在 `welinkSessionId?: string` 或 `string | number | null`
      - Test 2: API 适配与流式消费逻辑以 `number` 为唯一口径
      - Test 3: 会话分流比较逻辑不再依赖字符串与数字混比
    </behavior>
    <action>把 miniapp 协议类型、API 适配与 stream hook 中的 `welinkSessionId` 全部收敛到 `number`，删除已不需要的宽松兼容写法，并补上必要的类型或映射测试。</action>
    <verify>
      <automated>npm run typecheck --prefix skill-miniapp</automated>
    </verify>
    <done>前端消费层已经与后端数字型 `welinkSessionId` 完全对齐。</done>
  </task>

  <task type="auto" tdd="false">
    <name>Task 2: 同步协议文档与 gap tracking 结论</name>
    <files>
      documents/protocol-gap-tracking/
      documents/protocol/
      .planning/REQUIREMENTS.md
    </files>
    <behavior>
      - Test 1: 协议文档中与本 phase 直接相关的示例和字段表不再使用字符串型 `welinkSessionId`
      - Test 2: gap tracking 文档明确记录 08.1 已收口的范围与剩余 deferred 项
    </behavior>
    <action>更新协议与 gap tracking 文档，把 `welinkSessionId` 的标准类型统一记为后端 `Long`、前端 `number`，并记录本次 phase 对 Layer1、Layer2、miniapp 的收口范围和未纳入项。</action>
    <verify>
      <manual>抽查与 `welinkSessionId` 直接相关的协议章节和 gap tracking 文件，确认不存在新的字符串示例基线</manual>
    </verify>
    <done>代码、测试、文档对 `welinkSessionId` 的类型口径保持一致，后续审计不会再遇到同一类漂移。</done>
  </task>
</tasks>

<must_haves>
- 前端类型系统不能继续保留 `welinkSessionId` 的混合类型
- 文档必须反映本 phase 的最终基线，而不是保留旧示例误导后续开发
- 本 wave 只能在后端与中转链路完成后执行，避免前端先行收口导致调试混乱
</must_haves>

<success_criteria>
- `skill-miniapp` 中不再存在字符串型或混合型 `welinkSessionId`
- 相关协议文档和 gap tracking 文档完成同步
- Phase 08.1 可为后续 milestone audit 提供明确、可核查的基线
</success_criteria>
