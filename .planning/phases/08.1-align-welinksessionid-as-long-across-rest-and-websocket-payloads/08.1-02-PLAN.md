---
phase: 08.1
plan: 2
title: layer2 relay and pc-agent id type alignment
wave: 2
depends_on:
  - 1
requirements:
  - SESS-01
  - SAFE-02
files_modified:
  - ai-gateway/src/main/java/com/opencode/cui/gateway/
  - ai-gateway/src/test/java/com/opencode/cui/gateway/
  - src/main/pc-agent/
autonomous: true
user_setup: []
---

<objective>
在 `ai-gateway` 与 `pc-agent` 链路上消除剩余的字符串型 `welinkSessionId` 假设，保证跨服务 WebSocket 报文与中继错误回传继续兼容现有业务语义，但类型口径统一为数字。
</objective>

<context>
@.planning/phases/08.1-align-welinksessionid-as-long-across-rest-and-websocket-payloads/08.1-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>
  <task type="auto" tdd="true">
    <name>Task 1: 更新 gateway 测试夹具与消息断言</name>
    <files>
      ai-gateway/src/main/java/com/opencode/cui/gateway/model/GatewayMessage.java
      ai-gateway/src/test/java/com/opencode/cui/gateway/ws/SkillWebSocketHandlerTest.java
      ai-gateway/src/test/java/com/opencode/cui/gateway/service/EventRelayServiceTest.java
    </files>
    <behavior>
      - Test 1: Gateway WebSocket handler 解析 `welinkSessionId` 时以数字 JSON 为主路径
      - Test 2: `GatewayMessage` 的构造、序列化与 relay 测试都不再断言字符串型 `welinkSessionId`
      - Test 3: 现有消息路由与日志字段不因类型收敛而退化
    </behavior>
    <action>核对 `GatewayMessage` 及其周边测试，移除字符串型 `welinkSessionId` 的旧夹具，确保 gateway 端默认假设与 Phase 8 的 `Long` 模型一致。</action>
    <verify>
      <automated>mvn -pl ai-gateway test -Dtest=SkillWebSocketHandlerTest,EventRelayServiceTest</automated>
    </verify>
    <done>`ai-gateway` 代码和测试都以 `Long welinkSessionId` 为唯一基线。</done>
  </task>

  <task type="auto" tdd="true">
    <name>Task 2: 收紧 pc-agent 报文类型与错误回传</name>
    <files>
      src/main/pc-agent/EventRelay.ts
      src/main/pc-agent/__tests__/EventRelay.test.ts
    </files>
    <behavior>
      - Test 1: `EventRelay` 内部消息接口使用 `welinkSessionId?: number`
      - Test 2: `session_created`、错误回传与 tool 事件转发不会把数字 session id 转回字符串
      - Test 3: 现有 `toolSessionId` 字段仍保持字符串，不与本次整改混淆
    </behavior>
    <action>调整 `pc-agent` 的消息类型定义和相关单测，把 `welinkSessionId` 从字符串改为数字；保留必要的运行时容错，但避免任何新的字符串化输出。</action>
    <verify>
      <automated>npm test -- EventRelay.test.ts</automated>
    </verify>
    <done>Layer2 到 agent 的实际透传链路不再声明或输出字符串型 `welinkSessionId`。</done>
  </task>
</tasks>

<must_haves>
- `GatewayMessage.welinkSessionId` 的 `Long` 基线不能被测试夹具反向拉回字符串
- `pc-agent` 只能把 `toolSessionId` 当字符串，不能顺手把 `welinkSessionId` 也当字符串
- 类型整改不能破坏现有消息路由、错误回传与 `session_created` 链路
</must_haves>

<success_criteria>
- Layer2 WebSocket 报文与测试中不再存在字符串型 `welinkSessionId` 基线
- `ai-gateway` 与 `pc-agent` 的相关类型定义保持一致
- 现有 relay 行为在类型收敛后仍可自动化验证
</success_criteria>
